[project]
name = "rovarsprak"
version = "0.1.0"
description = "A Rövarspråk converter and decoder CLI tool"
authors = [
    { name = "Nicklas", email = "nicklas.thegerstrom@gmail.com" }
]
requires-python = ">=3.10"
readme = "README.md"

# Inga externa dependencies
dependencies = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project.scripts]
rovarsprak = "rovarsprak.main:main"